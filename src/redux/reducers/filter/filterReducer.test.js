/* global it */

/*
 *Created by Thomas on 16/04/2017.
 * Test file for filterReducer behavior
 */

import reducer from './filterReducer'
import types from './filterActionTypes'
import { changeCounty, clearCounty, setMinPrice, setMaxPrice, setNumberOfBedrooms, setMinSize, setMaxSize, clearFilter } from './filterActions.js'

it('should return the initial state', () => {
  changeCounty
})
